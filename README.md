# MiniCRM

MiniCRM is a simple CRM web application built using **Node.js, Express, EJS, and SQLite**.  
It allows users to manage clients, tasks, and invoices with a clean dashboard UI.

---

## 🚀 Features

- User login (session-based authentication)
- Dashboard with summary cards
- Client management (list, detail, add)
- Task management (add tasks per client)
- Invoice management
- SQLite database with seed data
- EJS templating + Bootstrap UI

---

## 🛠 Prerequisites

Make sure you have the following installed:

- **Node.js** (v16 or later recommended)
- **npm** (comes with Node.js)

Check versions:
```bash
node -v
npm -v
